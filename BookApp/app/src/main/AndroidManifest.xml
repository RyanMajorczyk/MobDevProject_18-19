<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ryma.bookapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/book_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/book_app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddBook.AddBookActivity" />
        <activity android:name=".AddReview.AddReviewActivity" />
        <activity android:name=".BookDetail.BookDetailActivity" />
        <activity android:name=".MyBooks.MyBooksActivity"></activity>
    </application>

</manifest>